{
  "name": "ethio-buna-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "admin": "npm run dev --workspace=admin-dashboard",
    "customer": "npm run dev --workspace=customer-menu"
  }
}
